From e57551396e40530c5636dad05f84e2eb14cd9e9a Mon Sep 17 00:00:00 2001
From: Nishidha Panpaliya <npanpa23@in.ibm.com>
Date: Wed, 15 Mar 2023 03:54:15 +0000
Subject: [PATCH] Remove extra null terminators

TF_CUDA_TOOLKIT_PATH will be stored as an
absolute path during the bazel build. Conda uses build paths that are
256 characters long. During conda install, it will replace the build path
with the shorter install path and append null terminators to keep the
binary file the same size. We need to use c_str() to remove the extra
null terminators.
---
 .../tsl/platform/default/cuda_libdevice_path.cc     | 13 ++++++++++++-
 1 file changed, 12 insertions(+), 1 deletion(-)

diff --git a/tensorflow/tsl/platform/default/cuda_libdevice_path.cc b/tensorflow/tsl/platform/default/cuda_libdevice_path.cc
index 646f9f01cb5..40a760b95d4 100644
--- a/tensorflow/tsl/platform/default/cuda_libdevice_path.cc
+++ b/tensorflow/tsl/platform/default/cuda_libdevice_path.cc
@@ -32,7 +32,18 @@ namespace tsl {
 std::vector<std::string> CandidateCudaRoots() {
 #if !defined(PLATFORM_GOOGLE)
   VLOG(3) << "CUDA root = " << TF_CUDA_TOOLKIT_PATH;
-  return {TF_CUDA_TOOLKIT_PATH, std::string("/usr/local/cuda")};
+  //return {TF_CUDA_TOOLKIT_PATH, std::string("/usr/local/cuda")};
+
+/* WML-CE Issue #11827 - TF_CUDA_TOOLKIT_PATH will be stored as an
+absolute path during the bazel build. Conda uses build paths that are
+256 characters long. During conda install, it will replace the build path
+with the shorter install path and append null terminators to keep the
+binary file the same size. We need to use c_str() to remove the extra
+null terminators.
+*/
+  string cuda_toolkit_path = TF_CUDA_TOOLKIT_PATH;
+  return {cuda_toolkit_path.c_str()};
+
 #else
   return {std::string("/usr/local/cuda")};
 #endif
-- 
2.34.1

