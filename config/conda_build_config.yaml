c_compiler_version:
  - 7.3.0                     
cxx_compiler_version:
  - 7.3.0                     
# One can change the value for below field on x86_64 if MKL is to be turned on.
use_mkl:
  - "false"
build_type:
  - "cpu"
  - "cuda"
tensorflow_select_version:
  - "1.0"
  - "2.0"
# tie the variant string to the select version
zip_keys:
  - build_type
  - tensorflow_select_version
